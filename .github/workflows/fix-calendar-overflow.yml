name: Fix mobile calendar overflow (legacy)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Calendar fix already integrated
        run: echo "El ajuste de calendario ya está integrado directamente en index.html."
